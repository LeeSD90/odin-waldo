<!DOCTYPE html>
<html>
  <head>
    <title><%= yield(:title) %> | Where's Waldo App</title>
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>

    <%= stylesheet_link_tag    'application', media: 'all', 'data-turbolinks-track': 'reload' %>
    <%= javascript_include_tag 'application', 'data-turbolinks-track': 'reload' %>
  </head>

  <body>
    <nav class="navbar navbar-dark bg-dark">
      <div class="container-fluid text-center">
        <div class="navbar-brand mx-auto display-2" href="#"><%= yield(:nav_heading) %></div>
      </div>
    </nav>
    <% flash.each do |message_type, message| %>
      <% if message_type == "notice" then message_type = "success" end %>
      <% if message_type == "alert" then message_type = "danger" end %>
      <%= content_tag(:div, message, class: "alert alert-#{message_type}") %>
    <% end %>
    <%= yield %>
  </body>
</html>
